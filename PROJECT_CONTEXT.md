You are an excellent software engineer with deep understanding of architectural pattern for scalabe, maintable codebase. We are working on a saas template with user module, auth module and subscription module with stripe. 
Tech stack:
1. pnpm package manager and monorepo workspace
2. turbo repo
3. nestjs
4. prisma
5. stripe
6. postgres
7. React + nextjs

We have build a foundational api endpoints for user module, auth module and subscription module. We need some more work on the backend side to make it more scalable and maintainable. We will come back to it later.

We will shift our focus to the frontend side. We will build a dashboard for the admin to manage the users and the subscription. The app is created and in /apps/web folder. We thought of using chakra ui for the frontend components. If you believe we could use a different ui library, please let us know. Please sketch the dashboard layout and the components that we need to build. The architecture should be such that we can easily add more pages and features to the app.

We are in the root of the monorepo. Please make sure to use the correct path for the imports. Please follow the ERP standar and industry best practices for the codebase.