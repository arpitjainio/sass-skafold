# Web

Next.js frontend for the SaaS Skafold starter.

## Local setup

From the repository root:

```bash
cp apps/web/.env.example apps/web/.env.local
pnpm --filter web dev
```

Default local URL:

```text
http://localhost:3000
```

For full workspace setup and known scaffold gaps, see:

- `README.md`
- `docs/usage.md`
